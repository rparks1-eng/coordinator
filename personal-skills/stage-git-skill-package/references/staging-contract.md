# Staging contract

Inputs: local approved repo, full commit SHA, exact skill ID, expected Git
archive SHA-256, inactive empty destination. Output: staged package and receipt. An
activation, copy to `~/.codex/skills`, or deletion of another skill is outside
this contract.
