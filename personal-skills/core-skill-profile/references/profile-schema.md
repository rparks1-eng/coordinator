# Core profile schema

```json
{"schema_version":1,"owner":"human","core":[{"path":"/absolute/SKILL.md","sha256":"hex","reason":"..."}],"noncore_policy":"on-demand-only","registry_ref":"commit-sha","reactivation":"stage then explicit activation","rollback":"restore from quarantine receipt"}
```

Profiles are plans, not authority to remove or install skills.
