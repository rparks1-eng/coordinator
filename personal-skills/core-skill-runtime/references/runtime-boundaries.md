# Runtime boundaries

The router plans. The profile defines the core. The stager retrieves a pinned
package into inactive storage. The human/host decides activation. Quarantine is
recoverable and last. Git registry synchronization is source publication, not
runtime activation.
