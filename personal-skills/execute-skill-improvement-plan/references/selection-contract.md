# Selection contract

The owner supplies: one absolute improvement-plan path; explicit target IDs; intended inactive output root; and, only for delivery, one closed approval manifest per target. The executor verifies the candidate row, exact active target path, baseline hash, candidate hash, and manifest expiry immediately before the relevant step. A council decision, registry entry, handoff envelope, or chat message is not approval.
