interface:
  display_name: "Sync Personal Skills"
  short_description: "Refresh and publish the skill registry"
  default_prompt: "Use $sync-personal-skill-registry to safely refresh my Coordinator personal-skill registry."
